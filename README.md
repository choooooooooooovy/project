# 🍜 美食分享网站

一个基于 React + Supabase 的美食菜谱分享平台，支持菜谱发布、浏览、评论和收藏功能。

## ✨ 功能特性

- 📖 **首页** - 浏览所有菜谱，支持分类筛选和搜索
- 📝 **发布菜谱** - 创建并分享你的美食菜谱
- 🔍 **菜谱详情** - 查看完整的食材、步骤和用户评论
- ⭐ **我的收藏** - 收藏喜欢的菜谱，随时查看
- 💬 **评论系统** - 对菜谱进行评分和评论
- 🏷️ **标签系统** - 通过标签快速找到相关菜谱

## 🗄️ 数据库设计

项目使用 Supabase 作为后端，包含 5 张数据表：

1. **users** - 用户信息
2. **recipes** - 菜谱信息
3. **comments** - 评论记录
4. **favorites** - 收藏记录
5. **tags** - 标签系统

## 🚀 快速开始

### 1. 配置 Supabase

1. 访问 [Supabase](https://supabase.com) 并创建新项目
2. 在 SQL Editor 中执行 `supabase_database_schema.sql` 文件中的所有 SQL 语句
3. 获取 API 密钥：
   - 进入项目设置 → API
   - 复制 `Project URL` 和 `anon public` key

### 2. 配置环境变量

1. 复制 `.env.example` 文件并重命名为 `.env`
2. 填入你的 Supabase 配置：

```env
VITE_SUPABASE_URL=你的_SUPABASE_URL
VITE_SUPABASE_ANON_KEY=你的_SUPABASE_ANON_KEY
```

### 3. 安装依赖并运行

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

## 📦 部署到 Netlify

### 方法 1：通过 Git 仓库部署（推荐）

1. 将代码推送到 GitHub
2. 登录 [Netlify](https://netlify.com)
3. 点击 "Add new site" → "Import an existing project"
4. 选择你的 GitHub 仓库
5. 配置构建设置：
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. 添加环境变量（Environment variables）：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
7. 点击 "Deploy site"

### 方法 2：手动部署

```bash
# 构建项目
npm run build

# 方式 A: 使用 Netlify CLI
npm install -g netlify-cli
netlify deploy --prod

# 方式 B: 直接拖拽
# 将 dist 文件夹拖拽到 Netlify 网站的部署区域
```

## 📁 项目结构

```
├── src/
│   ├── components/       # 可复用组件
│   │   ├── Navbar.jsx
│   │   └── RecipeCard.jsx
│   ├── pages/           # 页面组件
│   │   ├── Home.jsx            # 首页
│   │   ├── RecipeDetail.jsx    # 菜谱详情
│   │   ├── CreateRecipe.jsx    # 发布菜谱
│   │   └── Favorites.jsx       # 我的收藏
│   ├── services/        # API 服务
│   │   ├── recipeService.js
│   │   ├── commentService.js
│   │   ├── favoriteService.js
│   │   └── tagService.js
│   ├── lib/
│   │   └── supabase.js  # Supabase 客户端配置
│   ├── styles/          # 样式文件
│   ├── App.jsx
│   └── main.jsx
├── supabase_database_schema.sql  # 数据库脚本
├── package.json
└── vite.config.js
```

## 🛠️ 技术栈

- **前端框架**: React 18
- **构建工具**: Vite
- **路由**: React Router v6
- **后端服务**: Supabase
- **数据库**: PostgreSQL (Supabase)
- **部署平台**: Netlify

## 📝 开发说明

### 用户认证

当前版本使用模拟用户 ID (`demo-user-id`) 进行开发测试。实际生产环境中，需要集成 Supabase Auth：

```javascript
// 示例：集成 Supabase Auth
import { supabase } from './lib/supabase'

// 获取当前用户
const { data: { user } } = await supabase.auth.getUser()
```

### 图片上传

建议使用以下免费图片服务：
- [Unsplash](https://unsplash.com) - 高质量免费图片
- [Pexels](https://pexels.com) - 免费图片和视频
- Supabase Storage - 项目自带的文件存储（需配置）

## 🎨 页面预览

1. **首页** - 展示所有菜谱，支持分类和搜索
2. **菜谱详情页** - 完整的食材、步骤、评论
3. **发布菜谱页** - 表单式菜谱创建
4. **我的收藏页** - 个人收藏管理

## 📄 作业要求检查清单

- ✅ 使用 Supabase 作为后端数据库
- ✅ 使用 Netlify 部署前端
- ✅ 至少 3 个独立页面（实际开发了 4 个）
- ✅ Supabase 中至少 3 张数据表（实际设计了 5 张）
- ✅ 响应式设计，适配移动端
- ✅ 完整的 CRUD 功能

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题，请联系开发者。

---

**祝你使用愉快！** 🎉
