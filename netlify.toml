# Netlify 部署配置文件

[build]
  # 构建命令
  command = "npm run build"
  
  # 发布目录
  publish = "dist"

# 重定向规则 - 支持 React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量提示
# 请在 Netlify 网站后台添加以下环境变量：
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
