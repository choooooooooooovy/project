# 🚀 部署指南

## 📋 部署前准备

### 1. 配置 Supabase

#### 步骤 1：创建 Supabase 项目
1. 访问 [https://supabase.com](https://supabase.com)
2. 点击 "New Project"
3. 填写项目信息：
   - 项目名称：如 `food-sharing`
   - 数据库密码：设置一个强密码（请记住）
   - 区域：选择离你最近的区域（如 Singapore）
4. 等待项目创建完成（约 2 分钟）

#### 步骤 2：创建数据库表
1. 进入项目后，点击左侧菜单 **SQL Editor**
2. 点击 **New Query** 创建新查询
3. 打开 `supabase_database_schema.sql` 文件
4. **完整复制**所有内容，粘贴到 SQL 编辑器中
5. 点击 **Run** 按钮执行
6. 等待执行完成，应该会看到 "Success" 提示

#### 步骤 3：验证数据表
1. 点击左侧菜单 **Table Editor**
2. 确认以下 5 张表已创建：
   - ✅ users
   - ✅ recipes
   - ✅ comments
   - ✅ favorites
   - ✅ tags
3. 应该能看到测试数据（2 个用户、2 个菜谱等）

#### 步骤 4：获取 API 密钥
1. 点击左侧菜单 **Settings** → **API**
2. 找到以下信息：
   - **Project URL**: 类似 `https://xxxxx.supabase.co`
   - **anon public**: 一串很长的字符串
3. **保存这两个值**，稍后需要用到

---

## 🌐 部署到 Netlify

### 方法 1：通过 GitHub 自动部署（推荐）

#### 步骤 1：上传代码到 GitHub
```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Food Sharing Website"

# 创建 GitHub 仓库（在 GitHub 网站上）
# 然后关联并推送
git remote add origin https://github.com/你的用户名/food-sharing.git
git branch -M main
git push -u origin main
```

#### 步骤 2：连接 Netlify
1. 访问 [https://netlify.com](https://netlify.com)
2. 点击 **Add new site** → **Import an existing project**
3. 选择 **Deploy with GitHub**
4. 授权 Netlify 访问你的 GitHub
5. 选择刚才创建的仓库（如 `food-sharing`）

#### 步骤 3：配置构建设置
1. **Build command**: `npm run build`
2. **Publish directory**: `dist`
3. 点击 **Show advanced** → **New variable** 添加环境变量：

   | Key | Value |
   |-----|-------|
   | `VITE_SUPABASE_URL` | 你的 Supabase Project URL |
   | `VITE_SUPABASE_ANON_KEY` | 你的 Supabase anon public key |

4. 点击 **Deploy site**

#### 步骤 4：等待部署完成
- 部署过程大约 2-3 分钟
- 完成后会生成一个 URL，如 `https://amazing-curie-123456.netlify.app`
- 点击这个链接即可访问你的网站！

---

### 方法 2：手动部署（快速测试）

#### 步骤 1：创建 .env 文件
```bash
# 复制示例文件
cp .env.example .env
```

在 `.env` 文件中填入你的 Supabase 配置：
```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=你的_anon_key
```

#### 步骤 2：构建项目
```bash
# 安装依赖
npm install

# 构建
npm run build
```

#### 步骤 3：部署到 Netlify

**方式 A：使用 Netlify CLI**
```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录
netlify login

# 部署
netlify deploy --prod
```

**方式 B：拖拽上传**
1. 访问 [https://app.netlify.com/drop](https://app.netlify.com/drop)
2. 将 `dist` 文件夹直接拖拽到页面中
3. 等待上传完成
4. ⚠️ **重要**：手动添加环境变量
   - 进入站点设置 → **Environment variables**
   - 添加 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`
   - 重新部署

---

## ✅ 验证部署

### 1. 检查网站功能
- [ ] 首页能正常加载
- [ ] 能看到测试数据（宫保鸡丁、番茄炒蛋）
- [ ] 点击菜谱能进入详情页
- [ ] 发布菜谱功能正常
- [ ] 评论功能正常

### 2. 如果网站无法加载数据
1. 打开浏览器控制台（F12）
2. 查看 Console 是否有错误
3. 常见问题：
   - ❌ 提示 "请配置 SUPABASE_URL"
     - **解决**：检查环境变量是否正确设置
   - ❌ 提示 "Failed to fetch"
     - **解决**：检查 Supabase URL 是否正确
   - ❌ 提示 "Invalid API key"
     - **解决**：检查 Supabase anon key 是否正确

---

## 📝 提交作业

### 需要提交的内容
1. **Netlify 部署链接**
   - 示例：`https://your-site.netlify.app`
   
2. **GitHub 仓库链接**（如果使用 GitHub 部署）
   - 示例：`https://github.com/username/food-sharing`

3. **Supabase 项目截图**（可选）
   - 显示 5 张表的截图
   - 显示有测试数据

### 作业检查清单
- ✅ 网站正常运行，能访问所有页面
- ✅ 至少 3 个独立页面（实际有 4 个）
- ✅ Supabase 中至少 3 张表（实际有 5 张）
- ✅ 能发布新菜谱
- ✅ 能查看菜谱详情
- ✅ 能添加评论
- ✅ 响应式设计，手机端正常显示

---

## 🐛 常见问题

### Q1: npm install 失败
```bash
# 清除缓存后重试
npm cache clean --force
npm install
```

### Q2: 构建失败
```bash
# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Q3: Netlify 部署后环境变量不生效
- 在 Netlify 后台修改环境变量后，需要**重新部署**
- Site settings → Build & deploy → Trigger deploy

### Q4: 数据库连接失败
- 检查 Supabase 项目是否已暂停（免费版长时间不用会暂停）
- 进入 Supabase 项目，点击 "Resume" 恢复

### Q5: 如何自定义域名
1. 在 Netlify 中：Site settings → Domain management
2. 点击 "Add custom domain"
3. 按照提示配置 DNS

---

## 📞 获取帮助

遇到问题？
1. 检查浏览器控制台错误信息
2. 查看 Netlify 部署日志
3. 参考 `README.md` 文件
4. 联系老师或助教

---

**部署截止时间：2025年11月23日17:00**

祝你部署顺利！🎉
